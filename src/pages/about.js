import React from 'react';
import Layout from '../components/layout';
const AboutPage = () => {
  return(
    <Layout pageTitle = "About me">
      <p>Writing an about page is difficult task of its own</p>
    </Layout>
  )
};

export default AboutPage;
